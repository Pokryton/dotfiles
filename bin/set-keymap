#!/usr/bin/env bash

set -x

setxkbmap -option ""

case "${1:-v}" in
    v|vi)
        setxkbmap -option "caps:escape_shifted_capslock"
        ;;
    e|emacs)
        setxkbmap -option "ctrl:swapcaps"
        ;;
    n|no)
        ;;
esac
